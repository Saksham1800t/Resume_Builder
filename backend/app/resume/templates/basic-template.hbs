<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}} - Resume</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; padding: 0; }
        .container { max-width: 800px; margin: auto; padding: 20px; border: 1px solid #ddd; border-radius: 5px; }
        h1, h2 { color: #333; }
        .section { margin-bottom: 20px; }
        .section h2 { border-bottom: 2px solid #333; padding-bottom: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{name}}</h1>
        <p><strong>Email:</strong> {{email}} | <strong>Phone:</strong> {{phone}}</p>
        <p><strong>Address:</strong> {{address}}</p>

        <div class="section">
            <h2>Summary</h2>
            <p>{{summary}}</p>
        </div>

        <div class="section">
            <h2>Experience</h2>
            {{#each experience}}
            <p><strong>{{jobTitle}}</strong> - {{company}} ({{startDate}} - {{endDate}})</p>
            <p>{{description}}</p>
            {{/each}}
        </div>

        <div class="section">
            <h2>Education</h2>
            {{#each education}}
            <p><strong>{{degree}}</strong> - {{institution}} ({{year}})</p>
            {{/each}}
        </div>

        <div class="section">
            <h2>Skills</h2>
            <ul>
                {{#each skills}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
</body>
</html>
