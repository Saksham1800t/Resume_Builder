<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}} - Resume</title>
    <style>
        body { font-family: 'Helvetica Neue', sans-serif; margin: 0; padding: 0; background-color: #f4f4f4; }
        .resume { width: 800px; margin: 20px auto; background: white; padding: 20px; box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); }
        .header { background: #333; color: white; padding: 20px; text-align: center; }
        .header h1 { margin: 0; }
        .content { padding: 20px; }
        .section { margin-bottom: 20px; }
        .section h2 { border-bottom: 2px solid #333; padding-bottom: 5px; margin-bottom: 10px; }
        .info { display: flex; justify-content: space-between; }
        .skills ul { display: flex; flex-wrap: wrap; padding: 0; }
        .skills li { list-style: none; background: #ddd; margin: 5px; padding: 5px 10px; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="resume">
        <div class="header">
            <h1>{{name}}</h1>
            <p>{{email}} | {{phone}} | {{address}}</p>
        </div>
        <div class="content">
            <div class="section">
                <h2>Summary</h2>
                <p>{{summary}}</p>
            </div>

            <div class="section">
                <h2>Experience</h2>
                {{#each experience}}
                <div class="info">
                    <div><strong>{{jobTitle}}</strong> at {{company}}</div>
                    <div>{{startDate}} - {{endDate}}</div>
                </div>
                <p>{{description}}</p>
                {{/each}}
            </div>

            <div class="section">
                <h2>Education</h2>
                {{#each education}}
                <div class="info">
                    <div><strong>{{degree}}</strong> - {{institution}}</div>
                    <div>{{year}}</div>
                </div>
                {{/each}}
            </div>

            <div class="section skills">
                <h2>Skills</h2>
                <ul>
                    {{#each skills}}
                    <li>{{this}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
</body>
</html>
